package: graphql
author: Friends Of REDAXO
version: '1.0.0'

page:
    title: GraphQL
    perm: graphql[]
    icon: rex-icon fa-sliders

requires:
    php:
        version: '>=8.1, <8.3'
    redaxo: '>=5.14'
    packages:
        yrewrite: '>=2.9'

load: early
